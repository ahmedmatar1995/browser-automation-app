{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm run build"
  },
  "deploy": {
    "startCommand": "vite preview --port $PORT --host",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
